Create or refresh the daily marketing brief using the current project architecture.

Steps:
1. Read `config/agents.json`, `config/model-routing.json`, and `config/schedules.json`
2. Confirm the expected source agent outputs exist
3. Synthesize the marketing team brief and executive brief in the approved structure
4. Update any relevant memory or changelog entries if a stable pattern was discovered
