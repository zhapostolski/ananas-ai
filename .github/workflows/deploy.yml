name: Deploy to EC2

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  deploy:
    name: Deploy ${{ github.ref_name }} to EC2
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Deploy via SSM
        env:
          INSTANCE_ID: ${{ secrets.EC2_INSTANCE_ID }}
          DEPLOY_PATH: ${{ secrets.EC2_DEPLOY_PATH }}
          TAG: ${{ github.ref_name }}
        run: |
          python3 - <<'PYEOF'
          import boto3, os, sys, time

          instance_id = os.environ["INSTANCE_ID"]
          deploy_path = os.environ["DEPLOY_PATH"]
          tag = os.environ["TAG"]

          commands = [
              f"cd {deploy_path}",
              "git fetch origin --tags",
              f"git checkout {tag}",
              "source .venv/bin/activate",
              "pip install -e . --quiet",
              "python scripts/validate_pack.py",
              f"echo 'Deploy of {tag} complete'",
          ]

          ssm = boto3.client("ssm", region_name=os.environ.get("AWS_DEFAULT_REGION", "eu-central-1"))

          resp = ssm.send_command(
              InstanceIds=[instance_id],
              DocumentName="AWS-RunShellScript",
              Parameters={"commands": commands},
          )
          cmd_id = resp["Command"]["CommandId"]
          print(f"SSM command ID: {cmd_id}")

          # Poll until done (max 5 min)
          for _ in range(60):
              time.sleep(5)
              inv = ssm.get_command_invocation(CommandId=cmd_id, InstanceId=instance_id)
              status = inv["StatusDetails"]
              print(f"Status: {status}")
              if status in ("Success", "Failed", "Cancelled", "TimedOut"):
                  break

          print("STDOUT:", inv.get("StandardOutputContent", ""))
          print("STDERR:", inv.get("StandardErrorContent", ""))

          if status != "Success":
              print(f"Deploy failed with status: {status}")
              sys.exit(1)
          PYEOF

      - name: Install boto3
        if: false  # boto3 is pre-installed on ubuntu-latest
        run: pip install boto3

      - name: Notify on success
        if: success()
        run: echo "TODO - Teams notification"

      - name: Notify on failure
        if: failure()
        run: echo "TODO - Teams alert"
